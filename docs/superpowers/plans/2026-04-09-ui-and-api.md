# Streamlit UI & Backend API (Task 8) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the FastAPI backend endpoints and the Streamlit frontend UI.

**Architecture:** 
- **Backend (FastAPI):** Exposes `/recommend` and `/parse-transcript` endpoints.
- **Frontend (Streamlit):** Allows users to upload/paste transcript HTML and set preferences, then displays recommendations with score breakdowns.

**Tech Stack:** FastAPI, Streamlit, Pydantic, HTTPX.

---

### Task 1: Implement FastAPI Endpoints

**Files:**
- Create: `backend/main.py`

- [ ] **Step 1: Implement basic FastAPI app**

```python
from fastapi import FastAPI, HTTPException
from backend.models import Student, UserPreference, RecommendationResponse
from backend.scoring.orchestrator import HybridScorer
from backend.parser import parse_transcript_html

app = FastAPI()
scorer = HybridScorer()

@app.post("/recommend", response_model=RecommendationResponse)
async def get_recommendations(student: Student, preference: UserPreference):
    # Fetch courses from DB (implement a simple getter)
    # Call scorer.recommend
    pass

@app.post("/parse-transcript")
async def parse_transcript(html: str):
    return parse_transcript_html(html)
```

- [ ] **Step 2: Add course fetching logic**

Implement `get_all_courses()` in `backend/db.py` to fetch course data for the orchestrator.

---

### Task 2: Implement Streamlit UI

**Files:**
- Create: `frontend/app.py`

- [ ] **Step 1: Setup Streamlit Layout**

```python
import streamlit as st
import httpx

st.title("University Course Advisor")

# Sidebar for preferences
st.sidebar.header("Preferences")
difficulty = st.sidebar.slider("Target Difficulty", 0.0, 1.0, 0.5)
workload = st.sidebar.slider("Max Workload", 0.0, 1.0, 0.5)
interests = st.sidebar.text_input("Interests (comma separated)")

# Main area for transcript
transcript_html = st.text_area("Paste Transcript HTML here")

if st.button("Get Recommendations"):
    # 1. Parse transcript via backend
    # 2. Get recommendations via backend
    # 3. Display results
    pass
```

- [ ] **Step 2: Implement API calling logic**

- [ ] **Step 3: Implement Results Display (Cards/Breakdown)**

---
